name: 03 - Scheduled Cron

on:
  schedule:
    - cron: '0 8 * * 1-5'
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Health check at $(date -u) — all good."
